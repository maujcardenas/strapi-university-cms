'use strict';

/**
 * course-completion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::course-completion.course-completion');
